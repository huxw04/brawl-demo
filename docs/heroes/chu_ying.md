# 褚赢

状态：Q 版待机形象、完整第一版技能机制、Lab/战斗英雄选择、持久单位状态摘要和专项测试均已接入。当前先保证机制清晰，技能特效之后统一精修。

## 形象

运行资源为 `assets/heroes/chu_ying/sprites/chu_ying_idle_v1.png`。造型采用约三头身 Q 版比例，保留网剧褚赢最重要的识别点：高黑冠、长黑发、白色宽袖外袍、红色内袖与古装妆面。姿态保持空手落子前的从容感，棋子与棋盘由 3D 战斗特效生成，避免固定在角色贴图中。

生成母图与公开参考放在 `assets/heroes/chu_ying/references/`，来源和生成说明见同目录 `sources.md`。

## 当前数值

项目尺度为 `1 世界单位 = 100 码`。Q 的选点距离原设计未单独指定，第一版暂按 `500 码` 处理，便于 Lab 调试，后续可以独立调整。

- 普攻「飞棋」：在 `5.0 / 500 码` 内锁定敌人，发射会持续追踪目标的黑/白棋子，造成 2 伤害。
- Q「天元落子」：最多储备 3 枚，初始满层；指定位置从高空落下一枚棋子，落地对半径 `0.30 / 30 码` 造成 5 伤害，棋子在地面停留 10 秒。连续释放间隔 0.5 秒，每 5 秒恢复一枚，状态条直接显示当前棋子数。
- W「小棋盘」：在最多 `500 码` 的选点生成小棋盘，对半径 `0.50 / 50 码` 造成 10 伤害并击飞；棋盘 `500 码` 内己方已落地棋子飞向棋盘，分别对路径上的每名敌人造成 10 伤害。冷却 8 秒。
- E「棋魂传送」：获得霸体并锁定动作 1 秒，随后传送至 `10.0 / 1000 码` 内安全目标点；目标点处于墙体或地图外时沿路径回退至最近可站立位置。冷却 10 秒。
- R「棋界禁锢」：在 `500 码` 内选点，0.5 秒后创建以“施法时自身位置到选点”为直径的圆，圈内敌人被击飞 0.5 秒。触发时位于圈内的敌人在随后 15 秒内无法越过边界；后来进入的单位不追加禁锢。冷却 30 秒。

## 实现与确定性

- 落地棋子和禁锢圈是独立持久战斗单位，不依附英雄坐标；W 因此会牵引原地棋子，R 也不会随施法者移动。
- W 飞棋使用连续线段检测并记录已命中目标，同一枚棋子飞行期间不会重复伤害同一单位。
- 普攻投射物保存锁定目标；Q 充能计时、地面棋子、飞行棋子以及禁锢圈内名单全部进入 `BattleStateDigest`，为后续操作序列联机保留一致性检查。

## 测试

`tests/chu_ying_test.gd` 覆盖透明底资源、英雄目录、技能尺度、普攻追踪、Q 三层充能/释放间隔/落地伤害/十秒留场、W 路径伤害与棋子消耗、E 前摇霸体和传送、R 直径计算/击飞/初始名单/边界约束，以及持久单位状态摘要。
